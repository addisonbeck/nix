# Nix

## Installation

1. Fetch key
1. Sign in to iCloud
1. Run init script:

```bash
curl -s https://raw.githubusercontent.com/addisonbeck/nix/main/bootstrap.sh \
--output bootstrap.sh && \
bash bootstrap.sh
```
